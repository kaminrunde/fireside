{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "watch": "lerna run watch",
    "start:example": "lerna run start --scope='demo-project'",
    "start:app": "lerna run start --scope='fireside-app'",
    "start:storybook": "lerna run storybook --scope='demo-project'"
  }
}
