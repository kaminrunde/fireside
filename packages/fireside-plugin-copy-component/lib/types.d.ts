export declare type State = {
    [mediaSize: string]: {
        [row: number]: string;
    };
};
export declare type Options = {
    key: string;
    colors: {
        color: string;
        label: string;
    }[];
};
