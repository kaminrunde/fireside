import * as React from 'react';
import * as t from '../types';
declare type Props = {
    channel: t.Channel;
    api: any;
};
export default function Panel({ channel }: Props): React.JSX.Element;
export {};
